{
  "name": "micro-tool",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-wrangler.mjs && node scripts/sync-psu-counts.mjs && node scripts/add-reading-times.mjs || true",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name pc --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/preact": "^6.0.1",
    "@astrojs/sitemap": "^3.7.3",
    "@preact/signals": "^2.9.3",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.7",
    "chart.js": "^4.5.1",
    "preact": "^10.29.7",
    "tailwindcss": "^4.3.2"
  }
}