{
  "scripts": {
    "dev": "npm run --prefix micro-tool dev",
    "build": "npm run --prefix micro-tool build",
    "deploy": "npm run --prefix micro-tool deploy"
  },
  "dependencies": {
    "wrangler": "^4.110.0"
  }
}
